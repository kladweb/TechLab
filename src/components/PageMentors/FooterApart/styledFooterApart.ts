import styled from "styled-components";
import { backgroundColor } from "../../../styledConstantsColors";
export const StyledFooterApart = styled.div`
  padding-bottom: 24px;
  background: ${backgroundColor.black};
`